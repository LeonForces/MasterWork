# Contracts

- `event.schema.v1.json` - baseline RabbitMQ event payload contract (`schema_version = 1`)
- `openapi-v1.yaml` - static API contract snapshot for `v1` endpoints

Routing key format:

`event.<event_type>.v1`
